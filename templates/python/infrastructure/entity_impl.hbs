{{#each imports}}
from {{from}} import {{name}}
{{/each}}

class {{name}}Impl({{name}}):
{{#each properties}}
    {{name}}: {{type}}
{{/each}}

    def __init__(self, {{#each properties}}{{name}}: {{type}}{{#unless @last}}, {{/unless}}{{/each}}):
{{#each properties}}
        self.{{name}} = {{name}}
{{/each}}

{{#each methods}}
    {{#if (includes output "Promise")}}async {{else}}def {{/if}}{{name}}(self, {{#each inputs}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}) -> {{output}}:
        # 各実装をお願いします。
        pass

{{/each}}
