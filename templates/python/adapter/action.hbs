{{#each imports}}from {{from}} import {{name}}
{{/each}}

class {{name}}:
{{#each methods}}
    def {{name}}(self, {{#each inputs}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}) -> {{output}}:
        pass
{{/each}}
