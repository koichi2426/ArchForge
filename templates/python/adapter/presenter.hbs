{% for imp in imports %}
from {{ imp.from_ }} import {{ imp.name }}{% if imp.alias %} as {{ imp.alias }}{% endif %}
{% endfor %}

class {{ className }}({{ interfaceName }}):
    def output(self, {{ methodArg }}: {{ argType }}) -> {{ returnType }}:
        # 各実装をお願いします。
        pass
