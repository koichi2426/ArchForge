from domain.{{ name }} import {{ name }}
from domain.{{ name }}Repository import {{ name }}Repository
from .NoSQL import NoSQL

class {{ name }}NoSQL({{ name }}Repository):
    def __init__(self, db: NoSQL, collection_name: str):
        self._db = db
        self._collection_name = collection_name

{% for method in methods %}
    async def {{ method.name }}(self{% for input in method.inputs %}, {{ input }}{% endfor %}) -> {{ method.output }}:
        # 各実装をお願いします。
        pass
{% endfor %}
