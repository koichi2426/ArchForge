import { SQL, Rows, Row, Tx } from '../../adapter/repository/sql';

export class BaseSQL implements SQL {
  constructor(
    private readonly connectionString: string,
    private readonly options?: any
  ) {}

  async connect(): Promise<void> {
    // データベース接続の実装をお願いします。
  }

  async disconnect(): Promise<void> {
    // データベース切断の実装をお願いします。
  }

  async Execute(ctx: unknown, query: string, ...args: any[]): Promise<void> {
    // クエリ実行の実装をお願いします。
  }

  async Query(ctx: unknown, query: string, ...args: any[]): Promise<Rows> {
    // クエリ実行の実装をお願いします。
  }

  async QueryRow(ctx: unknown, query: string, ...args: any[]): Promise<Row> {
    // 単一行クエリ実行の実装をお願いします。
  }

  async BeginTransaction(ctx: unknown): Promise<Tx> {
    // トランザクション開始の実装をお願いします。
  }
} 