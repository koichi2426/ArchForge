import { {{name}} } from '../../domain/{{name}}';
import { {{name}}Repository } from '../../domain/{{name}}Repository';
import { SQL } from './SQL';

export class {{name}}SQL implements {{name}}Repository {
  constructor(
    private readonly db: SQL,
    private readonly tableName: string
  ) {}

  {{#each methods}}
  async {{name}}({{#each inputs}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}): Promise<{{output}}> {
    // 各実装をお願いします。
  }
  {{/each}}
}
