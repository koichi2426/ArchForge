export class {{name}}NoSQL implements {{name}} {
  constructor(
    private readonly db: NoSQL,
    private readonly collectionName: string
  ) {}

  {{#each methods}}
  async {{name}}({{#each inputs}}{{this}}{{#unless @last}}, {{/unless}}{{/each}}): Promise<{{output}}> {
    // 各実装をお願いします。
  }
  {{/each}}
}
